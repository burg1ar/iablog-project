spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/blogdb
    username: ai
    password: 11235
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
        temp:
          use_jdbc_metadata_defaults: false

jwt:
  config:
    secret: it-is-a-secret